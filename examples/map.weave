module Examples.Map

type List a =
  | Nil
  | Cons { head: a, tail: List a }

def map (f : a -> b) : List a -> List b =
  fold {
    Nil:  Nil,
    Cons: { head, tail } >>>
      fanout { head: head >>> f, tail: tail } >>> Cons,
  }
